package com.goldarte.mavlinkjoystick.utils

/**
 * Formats a Float to a string with a specified number of decimal places.
 * Uses platform-specific formatting tools.
 */
expect fun Float.format(decimals: Int): String
